const parrafo = document.querySelectorAll(`.destacados`);
console.log(parrafo);

const parrafos =document.querySelectorAll(`.destacados`);
console.log(parrafos);//que selecciona?

const parr = document.querySelectorAll(`p`);//selecciona todos
console.log(parr);

const primero =document.getElementById(`primero`);//selecciona el id de ese nombre y el primero que coincide con eso.
primero.style = `background:pink`;//a primero le doy un fondo rosa.
console.log(primero);



